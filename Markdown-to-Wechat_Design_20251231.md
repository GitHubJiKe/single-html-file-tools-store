# Markdown-to-Wechat Design 20251231

## 设计愿景与调性定义
- **核心调性**：专业、沉浸、高效、优雅。
- **设计愿景**：旨在为创作者提供一个如同专业 IDE 般的编辑环境，同时产出符合现代审美、适配公众号封闭生态的极致排版效果。视觉上追求“克制的美感”，通过细腻的阴影和色彩过渡提升工具的溢价感。

## 设计规范系统

### 1. 色彩体系 (Colors)
- **Primary (微信绿)**: `#07c160` (核心动作按钮)
- **Secondary (深灰)**: `#1e1e1e` (编辑器背景，专注模式)
- **Accent (主题色)**: 动态根据选定主题变化 (如：`#e67e22` 橙心, `#3498db` 蓝调)
- **Background**: `#f5f5f5` (全局背景)
- **Border**: `#ddd` (分割线)

### 2. 栅格与布局 (Layout)
- **编辑器/预览区**: 1:1 分栏布局。
- **Toolbar**: 顶部固定，高度 60px。
- **间隙**: 基础间隙 12px，预览区内边距 32px 以模拟公众号阅读环境。

### 3. 排版逻辑 (Typography)
- **编辑器字体**: `Fira Code`, `Consolas`, monospace (代码感)。
- **输出字体**: 优先采用系统字库，中文字符使用 `PingFang SC` 为首选，英文字符使用 `Segoe UI`，字号 16px，行高 1.75。

## 核心视图视觉解析
- **编辑器面板 (Dark Mode)**: 采用经典的 VS Code 深色调，代码高亮清晰，行间距舒适，减少长期输入的视觉疲劳。
- **工具栏 (Toolbar)**: 
  - 左侧：标题与状态标识。
  - 右侧：功能按钮堆叠。采用 Lucide 图标点缀，提升识别度。
  - 新增“图片本地化”按钮，采用闪电或下载图标。
- **多主题预览**: 
  - 主题切换时，预览区通过平滑过渡效果切换 CSS 类。
  - 每个主题有独特的标题装饰（如左边框、下划线、圆角点缀）。

## 关键交互与微动画说明
- **实时渲染**: 输入停止 300ms 后自动触发渲染，Mermaid 渲染时增加 Loading 占位符避免闪烁。
- **复制反馈**: 点击“复制到公众号”后，按钮显示“✅ 已复制”并伴随轻微缩放动画及 Toast 提示。
- **图片本地化**: 点击后展示进度条或图标旋转动画，完成后 Toast 提示“图片已转为 DataURL”。

## 前端开发实施建议
- **布局**: 使用 Flexbox 控制整体架构，`calc(100vh - 60px)` 确定内容区高度。
- **依赖管理**:
  - `marked.js`: 用于核心 Markdown 解析。
  - `mermaid.js`: 用于图表渲染 (注意：Mermaid 渲染后的 SVG 需要处理以确保复制到微信后依然有效，微信编辑器对 SVG 有一定限制，可能需要转为 DataURL 图片)。
  - `dom-to-image` 或 `html-to-image`: *可选*，若微信对某些复杂样式（如 Mermaid）不友好，可考虑将特定块转为图片插入。
- **图片转换**: 使用 `fetch` 获取图片 Blob，再用 `FileReader` 转为 Base64。需注意跨域 (CORS) 限制，若无法获取则提示用户。
