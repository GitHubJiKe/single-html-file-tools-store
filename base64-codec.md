# Base64 编解码工具

**文件名**: `base64-codec.html`  
**创建日期**: 2025-12-22  
**最后更新**: 2025-12-22  
**代码行数**: 490 行  

---

## 📋 工具简介

Base64 编解码工具，支持文本和文件的 Base64 编码解码，可处理图片、文档等各类文件，支持 Data URL 格式，提供一键复制和下载功能。

---

## 🎯 核心功能

### 功能列表
1. **文本编解码** - 文本与 Base64 互转
2. **文件编解码** - 文件与 Base64 互转
3. **Data URL 支持** - 识别和处理 Data URL 格式
4. **一键复制** - 复制编解码结果
5. **文件下载** - 下载 Base64 文本或解码后的文件

### 功能详情

#### 文本编解码
- **实现方式**: 使用 `btoa()` 和 `atob()` 进行编解码，支持 UTF-8
- **用户交互**: 输入文本，点击"编码"或"解码"按钮

#### 文件编解码
- **实现方式**: 使用 FileReader API 读取文件，转换为 Base64
- **用户交互**: 选择文件上传，或粘贴 Base64 字符串解码

#### Data URL 支持
- **实现方式**: 自动识别 `data:` 开头的 URL，提取 MIME 类型和 Base64 数据
- **用户交互**: 粘贴 Data URL，自动解析并解码

#### 一键复制
- **实现方式**: 使用 Clipboard API 复制文本
- **用户交互**: 点击"复制"按钮

#### 文件下载
- **实现方式**: 使用 Blob 和 URL.createObjectURL() 创建下载链接
- **用户交互**: 点击"下载"按钮

---

## 🔗 外部依赖

### CDN 链接
无外部依赖，完全使用原生 JavaScript 实现。

### 依赖说明
- **btoa() / atob()**: 浏览器内置的 Base64 编解码函数
- **FileReader API**: 读取本地文件
- **Blob API**: 创建文件对象
- **Clipboard API**: 复制功能

---

## 💻 核心实现逻辑

### 技术栈
- HTML5
- CSS3
- JavaScript (ES6+)
- FileReader API
- Blob API
- Clipboard API

### 关键代码实现

#### 1. 文本编码（支持 UTF-8）
```javascript
function encodeText(text) {
  try {
    // 使用 encodeURIComponent 和 escape 处理 Unicode 字符
    const encoded = btoa(unescape(encodeURIComponent(text)));
    return { success: true, data: encoded };
  } catch (error) {
    return { success: false, error: error.message };
  }
}
```
**实现说明**: `btoa()` 只能处理 ASCII 字符，对于 Unicode 字符（如中文），需要先用 `encodeURIComponent()` 转换为 UTF-8 字节序列，再用 `unescape()` 转换为 Latin-1 字符，最后用 `btoa()` 编码。

#### 2. 文本解码（支持 UTF-8）
```javascript
function decodeText(base64) {
  try {
    // 使用 escape 和 decodeURIComponent 处理 Unicode 字符
    const decoded = decodeURIComponent(escape(atob(base64)));
    return { success: true, data: decoded };
  } catch (error) {
    return { success: false, error: error.message };
  }
}
```
**实现说明**: 解码过程是编码的逆过程，先用 `atob()` 解码 Base64，再用 `escape()` 和 `decodeURIComponent()` 还原 Unicode 字符。

#### 3. 文件编码
```javascript
const reader = new FileReader();
reader.onload = function(e) {
  const base64 = e.target.result;  // Data URL 格式
  // base64 格式: data:image/png;base64,iVBORw0KGgo...
  displayResult(base64);
};
reader.readAsDataURL(file);
```
**实现说明**: 使用 FileReader 的 `readAsDataURL()` 方法读取文件，自动转换为 Data URL 格式（包含 MIME 类型和 Base64 数据）。

#### 4. 文件解码
```javascript
function decodeFile(base64String) {
  let base64Data = base64String;
  let mimeType = 'application/octet-stream';
  
  // 检查是否是 Data URL 格式
  if (base64String.startsWith('data:')) {
    const matches = base64String.match(/^data:([^;]+);base64,(.+)$/);
    if (matches) {
      mimeType = matches[1];
      base64Data = matches[2];
    }
  }
  
  // 解码 Base64
  const binaryString = atob(base64Data);
  const bytes = new Uint8Array(binaryString.length);
  for (let i = 0; i < binaryString.length; i++) {
    bytes[i] = binaryString.charCodeAt(i);
  }
  
  // 创建 Blob 并下载
  const blob = new Blob([bytes], { type: mimeType });
  downloadBlob(blob, 'decoded-file');
}
```
**实现说明**: 解析 Data URL 提取 MIME 类型和 Base64 数据，使用 `atob()` 解码为二进制字符串，转换为 Uint8Array，最后创建 Blob 对象用于下载。

### 数据流程

**文本编码流程:**
```
文本输入 → encodeURIComponent → unescape → btoa → Base64 输出
```

**文本解码流程:**
```
Base64 输入 → atob → escape → decodeURIComponent → 文本输出
```

**文件编码流程:**
```
文件选择 → FileReader.readAsDataURL → Data URL 输出
```

**文件解码流程:**
```
Base64 输入 → 解析 Data URL → atob → Uint8Array → Blob → 文件下载
```

### 状态管理
- **currentTab**: 当前选中的 Tab（文本/文件）
- **textInput**: 文本输入内容
- **textOutput**: 文本输出内容
- **fileEncodeOutput**: 文件编码结果
- **fileDecodeInput**: 文件解码输入

---

## 🎨 用户界面

### 布局结构
- **Tab 切换**: 文本编解码 / 文件编解码
- **文本模式**:
  - 输入文本框
  - 编码/解码/复制/清空按钮
  - 输出文本框
  - 调试面板（可选）
  
- **文件模式**:
  - 文件编码区域（选择文件、编码、复制、下载）
  - 文件解码区域（粘贴 Base64、解码并下载）

### 交互设计
- **Tab 切换**: 点击 Tab 切换文本/文件模式
- **文本编码**: 输入文本，点击"编码"
- **文本解码**: 输入 Base64，点击"解码"
- **文件编码**: 选择文件，点击"编码文件"
- **文件解码**: 粘贴 Base64，点击"解码并下载"

### 响应式设计
- **桌面端**: 标准布局，按钮横向排列
- **移动端**: 按钮自适应，文本框占满宽度

---

## 📝 使用说明

### 基本使用

#### 文本编码
1. 切换到"文本编解码" Tab
2. 在输入框输入文本（支持中文）
3. 点击"编码 (Encode)"按钮
4. 查看 Base64 结果，点击"复制结果"

#### 文本解码
1. 在输入框粘贴 Base64 字符串
2. 点击"解码 (Decode)"按钮
3. 查看解码后的文本

#### 文件编码
1. 切换到"文件编解码" Tab
2. 点击"选择文件"，选择要编码的文件
3. 点击"编码文件"按钮
4. 查看 Base64 结果，可复制或下载

#### 文件解码
1. 在"Base64 解码为文件"区域粘贴 Base64 字符串
2. 输入文件名（可选）
3. 点击"解码并下载"按钮
4. 文件自动下载

### 高级功能
- **Data URL 支持**: 粘贴 `data:image/png;base64,...` 格式，自动识别并解码
- **调试面板**: 勾选"显示调试信息"，查看编解码详情
- **URL 参数**: 通过 `?text=xxx` 参数自动填充文本

### 使用示例

**示例 1: 文本编码**
```
输入: Hello, 世界!
编码结果: SGVsbG8sIOS4lueVjCE=
```

**示例 2: 文本解码**
```
输入: SGVsbG8sIOS4lueVjCE=
解码结果: Hello, 世界!
```

**示例 3: 图片编码**
```
操作: 选择一张 PNG 图片
结果: data:image/png;base64,iVBORw0KGgoAAAANSUhEUgAA...
```

**示例 4: Data URL 解码**
```
输入: data:image/png;base64,iVBORw0KGgo...
操作: 点击"解码并下载"
结果: 下载 PNG 图片文件
```

---

## 🔧 技术细节

### API 使用
- **btoa() / atob()**: Base64 编解码
- **encodeURIComponent() / decodeURIComponent()**: UTF-8 编码处理
- **FileReader API**: 读取本地文件
  - `readAsDataURL()`: 读取为 Data URL
- **Blob API**: 创建文件对象
- **URL.createObjectURL()**: 创建下载链接
- **Clipboard API**: 复制功能

### 性能优化
- 使用原生 API，无需外部库
- 文件读取使用异步操作，不阻塞 UI
- 及时释放 Blob URL，避免内存泄漏

### 浏览器兼容性
| 浏览器 | 最低版本 | 支持情况 |
|--------|---------|---------|
| Chrome | 32+ | ✅ 完全支持 |
| Firefox | 26+ | ✅ 完全支持 |
| Safari | 7+ | ✅ 完全支持 |
| Edge | 12+ | ✅ 完全支持 |

**注意**: 
- btoa/atob 在所有现代浏览器中都有良好支持
- FileReader API 需要现代浏览器
- Clipboard API 需要 HTTPS 或 localhost 环境

---

## 🐛 已知问题

- [ ] 超大文件（> 50MB）编码可能导致浏览器卡顿
- [ ] 某些特殊字符的编解码可能需要额外处理

---

## 🚀 未来计划

- [ ] 添加批量文件编码
- [ ] 支持拖拽上传文件
- [ ] 添加编码格式选择（Base64/Base64URL）
- [ ] 支持其他编码格式（Hex、Binary）
- [ ] 添加文件预览功能
- [ ] 优化大文件处理性能

---

## 📊 代码统计

- **总行数**: 490 行
- **HTML**: ~50 行
- **CSS**: ~180 行
- **JavaScript**: ~260 行
- **注释**: ~0 行

**代码分布**:
- 样式定义: 37%
- 文本编解码: 25%
- 文件编解码: 25%
- 事件处理: 13%

---

## 🔍 关键词

`Base64`, `编码`, `解码`, `文件处理`, `Data URL`, `开发工具`, `单文件HTML`, `无构建`

---

## 📚 相关资源

- [Simon Willison 单文件哲学](https://simonwillison.net/2025/Dec/10/html-tools/)
- [Base64 编码原理](https://en.wikipedia.org/wiki/Base64)
- [MDN - btoa/atob](https://developer.mozilla.org/en-US/docs/Web/API/btoa)
- [FileReader API](https://developer.mozilla.org/en-US/docs/Web/API/FileReader)
- [Data URLs](https://developer.mozilla.org/en-US/docs/Web/HTTP/Basics_of_HTTP/Data_URLs)

---

*文档生成日期: 2025-12-22*  
*遵循单文件 HTML 工具开发规范*