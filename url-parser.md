# URL 解析工具

**文件名**: `url-parser.html`  
**创建日期**: 2025-12-22  
**最后更新**: 2025-12-22  
**代码行数**: 337 行  

---

## 📋 工具简介

URL 解析工具，验证 URL 合法性并解析为 JSON 结构，支持协议、主机、端口、路径、查询参数等完整解析，帮助开发者快速分析 URL 结构。

---

## 🎯 核心功能

### 功能列表
1. **URL 合法性验证** - 检查 URL 是否符合规范
2. **完整结构解析** - 解析为 JSON 格式
3. **查询参数解析** - 支持重复参数处理
4. **语法高亮显示** - JSON 结果带语法高亮
5. **一键复制** - 复制解析结果

### 功能详情

#### URL 合法性验证
- **实现方式**: 使用原生 URL API 进行验证
- **用户交互**: 输入 URL，点击"解析"按钮

#### 完整结构解析
- **实现方式**: 提取 URL 的各个组成部分（协议、主机、端口、路径等）
- **用户交互**: 自动解析并显示 JSON 结构

#### 查询参数解析
- **实现方式**: 使用 URLSearchParams API 解析查询字符串
- **用户交互**: 支持重复参数，显示为数组

#### 语法高亮
- **实现方式**: 正则表达式匹配 JSON 不同类型，添加 CSS 类
- **用户交互**: 自动应用，提升可读性

---

## 🔗 外部依赖

### CDN 链接
无外部依赖，完全使用原生 JavaScript 实现。

### 依赖说明
- **原生 URL API**: 使用浏览器内置的 URL 构造函数进行解析
- **URLSearchParams API**: 解析查询参数
- **原生 Clipboard API**: 复制功能

---

## 💻 核心实现逻辑

### 技术栈
- HTML5
- CSS3
- JavaScript (ES6+)
- 原生 URL API
- URLSearchParams API
- Clipboard API

### 关键代码实现

#### 1. URL 解析
```javascript
function parseURL(urlString) {
  try {
    const url = new URL(urlString);
    return {
      valid: true,
      href: url.href,
      protocol: url.protocol,
      hostname: url.hostname,
      port: url.port || null,
      pathname: url.pathname,
      search: url.search,
      hash: url.hash,
      origin: url.origin,
      searchParams: parseSearchParams(url.searchParams)
    };
  } catch (error) {
    return {
      valid: false,
      error: error.message
    };
  }
}
```
**实现说明**: 使用原生 URL 构造函数解析 URL。如果 URL 格式错误，会抛出异常，捕获后返回错误信息。成功则提取各个组成部分。

#### 2. 查询参数解析
```javascript
function parseSearchParams(searchParams) {
  const params = {};
  for (const [key, value] of searchParams.entries()) {
    if (params[key]) {
      // 重复参数，转换为数组
      if (Array.isArray(params[key])) {
        params[key].push(value);
      } else {
        params[key] = [params[key], value];
      }
    } else {
      params[key] = value;
    }
  }
  return params;
}
```
**实现说明**: 遍历 URLSearchParams 对象，处理重复参数。如果同一个键出现多次，将值转换为数组存储。

#### 3. 语法高亮
```javascript
function syntaxHighlight(json) {
  json = json.replace(/&/g, '&amp;').replace(/</g, '&lt;').replace(/>/g, '&gt;');
  return json.replace(/("(\\u[a-zA-Z0-9]{4}|\\[^u]|[^\\"])*"(\s*:)?|\b(true|false|null)\b|-?\d+(?:\.\d*)?(?:[eE][+\-]?\d+)?)/g, 
    function (match) {
      let cls = 'number';
      if (/^"/.test(match)) {
        if (/:$/.test(match)) {
          cls = 'key';
        } else {
          cls = 'string';
        }
      } else if (/true|false/.test(match)) {
        cls = 'boolean';
      } else if (/null/.test(match)) {
        cls = 'null';
      }
      return '<span class="' + cls + '">' + match + '</span>';
    });
}
```
**实现说明**: 使用正则表达式匹配 JSON 中的不同类型，为每种类型添加对应的 CSS 类名，实现语法高亮。

### 数据流程
```
用户输入 URL → URL API 验证 → 提取各部分 → 解析查询参数 → 生成 JSON → 语法高亮 → 显示结果
```

### 状态管理
- **inputURL**: 用户输入的原始 URL 字符串
- **parsedData**: 解析后的 URL 结构对象
- **isValid**: URL 是否合法

---

## 🎨 用户界面

### 布局结构
- **顶部**: 标题和说明
- **输入区域**: 文本框，用于输入 URL
- **操作按钮**: 解析、复制、清空
- **输出区域**: 显示解析后的 JSON，支持语法高亮
- **调试面板**: 可选，显示详细信息

### 交互设计
- **解析**: 点击按钮或按 Ctrl+Enter，解析 URL
- **复制**: 点击按钮，复制 JSON 结果到剪贴板
- **清空**: 点击按钮，清空输入和输出
- **调试**: 勾选复选框，显示调试信息

### 响应式设计
- **桌面端**: 标准布局，按钮横向排列
- **移动端**: 按钮自适应，文本框和输出区域占满宽度

---

## 📝 使用说明

### 基本使用
1. 在输入框中粘贴或输入 URL
2. 点击"解析"按钮（或按 Ctrl+Enter）
3. 查看解析后的 JSON 结构
4. 点击"复制"按钮，复制到剪贴板

### 高级功能
- **URL 参数传递**: 通过 `?url=xxx` 参数自动解析
- **调试面板**: 勾选"显示调试信息"，查看详细解析过程
- **快捷键**: 使用 Ctrl+Enter 快速解析

### 使用示例

**示例 1: 简单 URL**
```
输入: https://example.com
解析结果:
{
  "valid": true,
  "href": "https://example.com/",
  "protocol": "https:",
  "hostname": "example.com",
  "port": null,
  "pathname": "/",
  "search": "",
  "hash": "",
  "origin": "https://example.com",
  "searchParams": {}
}
```

**示例 2: 完整 URL**
```
输入: https://user:pass@example.com:8080/path?key=value#hash
解析结果:
{
  "valid": true,
  "protocol": "https:",
  "hostname": "example.com",
  "port": "8080",
  "pathname": "/path",
  "search": "?key=value",
  "hash": "#hash",
  "searchParams": {
    "key": "value"
  }
}
```

**示例 3: 重复参数**
```
输入: https://example.com?tag=a&tag=b&tag=c
解析结果:
{
  "searchParams": {
    "tag": ["a", "b", "c"]
  }
}
```

**示例 4: 非法 URL**
```
输入: not-a-valid-url
解析结果:
{
  "valid": false,
  "error": "Invalid URL"
}
```

---

## 🔧 技术细节

### API 使用
- **URL API**: 解析和验证 URL
  - `new URL()`: 构造 URL 对象
  - `url.protocol`, `url.hostname` 等: 访问各个组成部分
  
- **URLSearchParams API**: 解析查询参数
  - `url.searchParams.entries()`: 遍历所有参数
  
- **Clipboard API**: 复制功能
  - `navigator.clipboard.writeText()`: 复制文本

### 性能优化
- 使用原生 API，无需外部库，加载速度快
- 语法高亮使用正则表达式一次性处理
- 支持 URL 参数传递，方便分享

### 浏览器兼容性
| 浏览器 | 最低版本 | 支持情况 |
|--------|---------|---------|
| Chrome | 32+ | ✅ 完全支持 |
| Firefox | 26+ | ✅ 完全支持 |
| Safari | 7+ | ✅ 完全支持 |
| Edge | 12+ | ✅ 完全支持 |

**注意**: URL API 在所有现代浏览器中都有良好支持。

---

## 🐛 已知问题

- [ ] 某些特殊字符的 URL 可能需要编码处理
- [ ] 超长 URL（> 2000 字符）可能影响显示

---

## 🚀 未来计划

- [ ] 添加 URL 编码/解码功能
- [ ] 支持批量 URL 解析
- [ ] 添加 URL 构建功能（反向操作）
- [ ] 支持更多 URL 格式（mailto、tel 等）
- [ ] 添加 URL 参数编辑功能
- [ ] 支持导出为不同格式（CSV、XML）

---

## 📊 代码统计

- **总行数**: 337 行
- **HTML**: ~40 行
- **CSS**: ~120 行
- **JavaScript**: ~175 行
- **注释**: ~2 行

**代码分布**:
- 样式定义: 36%
- URL 解析: 30%
- 语法高亮: 20%
- 事件处理: 14%

---

## 🔍 关键词

`URL`, `解析`, `验证`, `查询参数`, `开发工具`, `单文件HTML`, `无构建`, `原生API`

---

## 📚 相关资源

- [Simon Willison 单文件哲学](https://simonwillison.net/2025/Dec/10/html-tools/)
- [URL API 文档](https://developer.mozilla.org/en-US/docs/Web/API/URL)
- [URLSearchParams API](https://developer.mozilla.org/en-US/docs/Web/API/URLSearchParams)
- [URL 规范](https://url.spec.whatwg.org/)

---

*文档生成日期: 2025-12-22*  
*遵循单文件 HTML 工具开发规范*