# 📋 表单构建器

一个功能丰富的本地表单管理系统，采用 Local-First 原则，所有数据本地存储，支持离线使用。

## ✨ 主要功能

### 1. 表单设计（📐 表单设计）
- **拖拽式设计**：从左侧控件库拖拽控件到画布
- **10种控件类型**：
  - 📝 单行文本
  - 📄 多行文本
  - 🔢 数字
  - ✉️ 邮箱
  - ☑️ 复选框
  - 🔘 单选按钮
  - 📋 下拉选择
  - 📅 日期
  - 🕐 时间
  - ⭐ 评分

- **属性编辑**：
  - 字段标签自定义
  - 必填项设置
  - 占位符文本
  - 长度限制（文本）
  - 数值范围（数字）
  - 选项管理（复选框/单选/下拉）
  - 帮助文本

- **表单操作**：
  - 保存表单到本地
  - 预览表单效果
  - 删除字段

### 2. 表单列表（📑 表单列表）
- 查看所有已创建的表单
- 显示字段数和修改时间
- 编辑表单
- 发布表单
- 删除表单

### 3. 数据收集（💾 数据收集）
- **表单发布**：发布表单供用户填写
- **数据收集**：自动收集用户提交的数据
- **数据过滤**：按表单筛选数据
- **数据查看**：查看单条提交数据的详细信息
- **数据删除**：删除不需要的数据
- **导出Excel**：将所有数据导出为Excel文件
- **导入数据**：从JSON文件导入备份数据

### 4. 数据分析（📊 数据分析）
- **统计信息**：
  - 总提交数
  - 表单总数
  
- **可视化图表**：
  - 表单提交统计（柱状图）
  - 支持ECharts图表库

### 5. 数据导出
- **导出Excel**：支持多格式导出
- **导出JSON**：完整数据备份
- **导入功能**：恢复备份数据

## 🏗️ 架构设计

### 数据存储
- **IndexedDB**：本地数据库存储
- **表单存储**：存储表单配置和结构
- **响应存储**：存储用户提交的数据
- **表单与响应关联**：通过formId建立关系

### 数据结构

#### 表单对象
```javascript
{
  id: number,                    // 表单ID
  name: string,                  // 表单名称
  fields: Array,                 // 字段数组
  createdAt: string,             // 创建时间
  updatedAt: string              // 更新时间
}
```

#### 字段对象
```javascript
{
  id: string,                    // 字段ID（field_timestamp）
  type: string,                  // 字段类型
  label: string,                 // 字段标签
  required: boolean,             // 是否必填
  placeholder: string,           // 占位符
  options: Array,                // 选项（复选框/单选/下拉）
  defaultValue: string,          // 默认值
  helpText: string,              // 帮助文本
  minLength: string,             // 最小长度
  maxLength: string,             // 最大长度
  min: string,                   // 最小值
  max: string                    // 最大值
}
```

#### 响应对象
```javascript
{
  id: number,                    // 响应ID
  formId: number,                // 关联的表单ID
  data: Object,                  // 提交的数据
  submittedAt: string            // 提交时间
}
```

## 🎯 使用流程

### 创建表单
1. 进入"表单设计"标签页
2. 输入表单名称
3. 从左侧拖拽控件到画布
4. 点击控件进行属性编辑
5. 点击"保存表单"

### 发布表单
1. 进入"表单列表"标签页
2. 找到要发布的表单
3. 点击"发布"按钮
4. 填写表单并提交

### 查看数据
1. 进入"数据收集"标签页
2. 可选择按表单筛选
3. 点击"查看"查看单条数据
4. 点击"导出Excel"导出所有数据

### 数据分析
1. 进入"数据分析"标签页
2. 查看统计信息
3. 查看可视化图表

## 🔧 技术栈

- **HTML5**：页面结构
- **CSS3**：样式设计（响应式）
- **JavaScript**：核心逻辑
- **IndexedDB**：本地数据库
- **ECharts**：数据可视化
- **SheetJS**：Excel导出

## 💾 本地存储

### IndexedDB 数据库
- 数据库名：`FormBuilderDB`
- 版本：1
- 存储对象：
  - `forms`：表单存储
  - `responses`：响应存储

### 索引
- `forms`：`name`、`createdAt`
- `responses`：`formId`、`submittedAt`

## 🌐 Local-First 原则

- ✅ 所有数据本地存储，不上传到服务器
- ✅ 支持完全离线使用
- ✅ 支持数据导出备份
- ✅ 支持数据导入恢复
- ✅ 用户数据完全掌控

## 📱 响应式设计

- 桌面端：三栏布局（控件库 + 画布 + 属性编辑）
- 平板/手机：自动隐藏侧栏

## 🎨 UI 特点

- 现代化设计
- 渐变色背景
- 平滑过渡动画
- 直观的交互反馈
- Toast提示通知
- 模态框对话框

## 🚀 快速开始

1. 在浏览器中打开 `form-builder.html`
2. 创建第一个表单
3. 添加字段并配置属性
4. 保存表单
5. 发布表单供用户填写
6. 查看和分析收集的数据

## 📝 注意事项

- 所有数据存储在浏览器的IndexedDB中
- 清除浏览器缓存会导致数据丢失
- 建议定期导出数据备份
- 不同浏览器的数据不互通
- 同一浏览器的不同标签页可以共享数据

## 🔐 数据安全

- 数据完全存储在本地浏览器
- 不经过任何服务器
- 支持本地备份和恢复
- 用户对数据有完全控制权

## 🎓 学习资源

- IndexedDB API：https://developer.mozilla.org/en-US/docs/Web/API/IndexedDB_API
- ECharts：https://echarts.apache.org/
- SheetJS：https://sheetjs.com/

## 📄 许可证

开源免费使用