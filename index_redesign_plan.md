# index.html UI 重构方案：Zen Minimalist (禅意极简)

| 属性           | 内容                                                  |
| :------------- | :---------------------------------------------------- |
| **设计风格**   | **Swiss Modern / Bento Grid (瑞士现代 / 模块化网格)** |
| **核心关键词** | 留白艺术、极简轮廓、精准排版、去装饰化                |
| **重构目标**   | 移除“AI 塑料感”的渐变与过度阴影，追求纸面般的质感。   |

## 1. 审美方向转换

- **From**: 紫色渐变背景、圆润大阴影、居中喧闹的布局。
- **To**: 纯粹的浅灰/米白背景（或纯黑）、硬朗的边框线、精准的非对称布局。

## 2. 视觉规范重定义 (Design Tokens)

### 2.1 色彩与质感

- **Background**: `#F8FAFC` (极为清冷的灰白色)
- **Border**: `#E2E8F0` (极细的边框，代替阴影)
- **Primary Text**: `#0F172A` (深海军蓝接近黑)
- **Secondary Text**: `#64748B` (理性灰)
- **Accent**: `#2563EB` (克制的皇家蓝，仅用于交互点睛)

### 2.2 字体排印 (Typography)

- **Title**: 使用 `Outfit` (几何感) 或 `Inter` (功能感)。增加 `letter-spacing: -0.02em` 以提升高级感。
- **Layout**: 采用固定间距（Spans of 8px），消除视觉上的混乱。

## 3. 核心视图重构方案

- **Header**: 靠左布局，标题采用巨大的、加粗的黑体文字。取消副标题的装饰感，改为一句话说明。
- **Search**: 细长的、只有下划线或极细边框的输入框。
- **Tool Card**:
    - 取消投影。
    - 使用 `border: 1px solid #E2E8F0`。
    - 增加内边距（Padding 24px+）。
    - 鼠标悬停（Hover）时，边框颜色加深，并伴随极轻微的背景色变化，而非浮起感。
- **Tags**: 采用统一的浅灰色小方块，无圆角或极小圆角。

## 4. 前端实现调整

- **边框处理**: 使用 `outline` 或 `box-shadow` 模拟细边框。
- **动画**: 仅保留平滑的透明度切换和极短的平移。
- **布局**: 使用 `grid-template-areas` 构建更有秩序感的网格。
